# NZBHydra2 Configuration
# URL to your NZBHydra2 instance
NZBHYDRA_URL=http://192.168.1.203:5076

# NZBHydra2 API Key (optional, only if NZBHydra2 has authentication enabled)
# Leave empty if NZBHydra2 has no authentication
NZBHYDRA_API_KEY=

# Seedable API Key
# This is the API key that Sonarr/Radarr will use to connect to Seedable
# Generate a random string or use a password manager
API_KEY=your-api-key-here

# Radarr/Sonarr API Configuration (optional but highly recommended)
# Used to convert IMDb/TVDb IDs to movie/show titles for efficient searching
# Without these, searches may return 800+ irrelevant results
# Find API keys in your Radarr/Sonarr: Settings → General → Security → API Key
RADARR_URL=http://localhost:7878
RADARR_API_KEY=

SONARR_URL=http://localhost:8989
SONARR_API_KEY=

# Filtering Settings
# Minimum number of trackers a torrent must be on to be included in results
# Default: 2 (torrent must exist on at least 2 trackers)
MIN_DUPLICATES=2

# Size tolerance percentage for grouping similar torrents
# Allows for slight variations in file size (e.g., different compression)
# Default: 2.0 (allows ±2% size difference)
SIZE_TOLERANCE_PERCENT=2.0

# Private Tracker Prioritization (optional)
# Comma-separated list of private tracker names as they appear in NZBHydra2
# If set, public trackers will only show results that can cross-seed to private trackers
# Groups with ONLY public trackers will be filtered out
# Example: TorrentLeech,IPTorrents,PassThePopcorn,BroadcasTheNet
PRIVATE_TRACKERS=

# Server Configuration
# Port to run the service on
PORT=5000

# Host to bind to (0.0.0.0 allows external connections)
HOST=0.0.0.0
